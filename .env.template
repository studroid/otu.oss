# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api

# 모바일에서 접속을 허용하려면 개발 머신의 IP를 입력하고, 모바일로 접속 한 후에, 이메일로 로그인
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_HOST=http://localhost:3000
NEXT_PUBLIC_SOCIAL_LOGIN_REDIRECT_TO=http://localhost:3000

NEXT_PUBLIC_SUPABASE_ANON_KEY=example.example.example
SUPABASE_SERVICE_ROLE_KEY=example.example.example
SUPABASE_DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres
# github.com OAuth 앱 설정에서 발급받은 값을 입력
SUPABASE_AUTH_GITHUB_CLIENT_ID=example_github_client_id
SUPABASE_AUTH_GITHUB_SECRET=example_github_secret
# 개발환경에서 소셜 로그인이 끝나고 supabase 인증을 처리할 콜백 주소로 이 주소와 github의 callback url 주소가 같아야 함. 
SUPABASE_AUTH_GITHUB_CALLBACK_URL_FOR_DEV=http://localhost:54321/auth/v1/callback
NEXT_PUBLIC_SUPABASE_AUTH_DEBUG_ENABLED=false

# egoing에게 문의
AWS_SQS_ACCESS_KEY_ID=EXAMPLESL7GXK2WJUFPVJ
AWS_SQS_ACCESS_KEY_SECRET=kablFfFZ4E2y9NrQiEXAMPLEJuDMUqCtzLRmJ9c
AWS_SQS_QUEUE_URL=https://sqs.ap-northeast-2.amazonaws.com/EXAMPLE/otu-ai-dev

# AI 기능 설정
# ENABLE_AI가 true일 때만 AI 기능(채팅, 제목 생성, 이미지 분석, 임베딩)이 활성화됩니다.
# 오픈소스 배포 시 기본값 false로 설정하여 API 키 없이도 앱이 정상 동작합니다.
ENABLE_AI=false

# egoing에게 문의
OPENAI_API_KEY=sk-example
OPENAI_ORGANIZATION_ID=example_org-RVScaqyivcePfOVyS6ClVBna
UPLOADCARE_PRIVATE_KEY=example_b8afe236f1vb6cdf4afa9
NEXT_PUBLIC_UPLOADCARE_PUBLIC_KEY=example_d3463533944c
# COHERE_API_KEY는 더 이상 사용되지 않습니다. 임베딩은 Vercel AI Gateway를 통해 처리됩니다.

NEXT_PUBLIC_SENTRY_PROJECT="dev-otu-ai"
NEXT_PUBLIC_SENTRY_DSN=https://example@example.ingest.sentry.io/example
# Sentry 활성화 여부 (true: 에러 모니터링 활성화, false: 비활성화)
# 오픈소스 배포 시 기본값 false, 프로덕션에서는 true로 설정
NEXT_PUBLIC_ENABLE_SENTRY=false

NEXT_PUBLIC_ANDROID_sha256_cert_fingerprints=XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX:XX # keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey 로 확인

# DEBUG=  # 개발 시 필요한 로거 활성화 (예: sync,chat,editor,alarm)
NEXT_PUBLIC_ENABLE_JOTAI_DEVTOOLS=false # jotai-devtools 켬/끔
#test